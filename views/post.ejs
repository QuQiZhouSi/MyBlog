<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.13.0/katex.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.13.0/katex.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/markdown-it/12.0.4/markdown-it.min.js"></script>
</head>
<body>
    <!-- ?? div ??示解析后的 Markdown 和 LaTeX ?容 -->
    <div id="content"><%= content %></div>

    <script>
        // 使用 markdown-it 渲染????的 Markdown ?容
        const md = window.markdownit();

        // ?取??的 Markdown ?容
        const markdownContent = document.getElementById('content').innerHTML;

        // 渲染 Markdown ? HTML
        document.getElementById('content').innerHTML = md.render(markdownContent);

        // ?理 LaTeX 公式渲染
        document.querySelectorAll('script[type^="math/tex"]').forEach((el) => {
            const display = el.type.includes('mode=display');
            const math = el.textContent || el.innerText;
            katex.render(math, el, { displayMode: display });
        });
    </script>
</body>
</html>
