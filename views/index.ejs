<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>博客主页</title>
    <!-- 设置浏览器标签页的favicon图标 -->
    <link rel="icon" type="image/png" href="/images/favicon.png"> <!-- 替换为您favicon图片的路径 -->
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-image: url('/images/background.jpg'); /* 替换为全局背景图片路径 */
            background-size: cover;
            background-position: center center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }

        /* 顶部背景图片区域 */
        .header {
            background-image: url('/images/your-header-background.jpg'); /* 替换为顶部背景图片路径 */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            height: 400px;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            color: white;
            text-align: center;
        }

        /* logo 样式 */
        .logo {
            position: absolute;
            top: 20px; /* 调整logo在顶部的位置 */
            left: 20px; /* 调整logo在左侧的位置 */
        }

        .logo img {
            max-width: 100px; /* 控制logo的宽度 */
            height: auto;
        }

        .header h1 {
            font-size: 48px;
            font-weight: bold;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.7);
        }

        .container {
            max-width: 900px;
            margin: -100px auto 0 auto;
            background-color: rgba(255, 255, 255, 0.9);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: left;
        }

        h2 {
            text-align: center;
            margin-top: 0;
        }

        .article-list {
            list-style: none;
            padding: 0;
        }

        .article-list li {
            margin-bottom: 20px;
        }

        .article-title {
            font-size: 24px;
        }

        .article-summary {
            font-size: 16px;
            color: #666;
        }

        .article-list a {
            text-decoration: none;
            color: #333;
        }

        .article-list a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

    <!-- 顶部背景图片区域 -->
    <div class="header">
        <h1>欢迎来到曲奇丶宙斯の小屋</h1>
    </div>

    <!-- 下方内容区域 -->
    <div class="container">
        <h2>文章列表</h2>
        <ul class="article-list">
            <% articles.forEach(function(article) { %>
                <li>
                    <a href="/post/<%= encodeURIComponent(article.filename) %>">
                        <h3 class="article-title"><%= article.title %></h3>
                    </a>
                    <p class="article-author"><%= article.author %></p>
                    <p class="article-summary"><%= article.summary %></p>
                    <p class="article-tag"><%= article.tag %></p>
                    
                </li>
            <% }); %>
        </ul>
    </div>

</body>
</html>
